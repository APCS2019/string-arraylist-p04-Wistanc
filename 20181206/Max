public String decodeString (ArrayList<StringPart> parts)
public int findNthOccurrence(String str, int n){
      int loc = -1;
      int count = 0;
      int strat =0;
      int sub = -1;
      while(str.substring(start).indexOf(str)){
           count++;
           if (count == n){
                return loc;
           }
            start = loc + 1;
          }
           return -1;
